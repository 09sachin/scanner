import { Scanner } from '@/components/Scanner';

export default function ScannerPage() {
  return <Scanner />;
}

export const metadata = {
  title: 'Scanner - Barcode & QR Code Reader',
  description: 'Scan barcodes and QR codes from images or camera. Upload files or use live camera scanning.',
}; 